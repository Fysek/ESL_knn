#include <cstdlib>
#include <string>
#include "MNISTread.h"

void distance(std::vector<std::vector <double>> &vec);
void assign_label(std::vector <double > &vec);
void compare(std::vector <double > &assigned_labels_vec);